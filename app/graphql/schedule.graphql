query Schedule($page: Int, $startDate: Int!) {
  Page(page: $page) {
    airingSchedules(airingAt_greater: $startDate, sort: TIME) {
      ...airing
    }
    pageInfo {
      currentPage
      hasNextPage
    }
  }
}
