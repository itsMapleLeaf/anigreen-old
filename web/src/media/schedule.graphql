query Schedule($todaySeconds: Int!, $page: Int) {
	Page(page: $page) {
		airingSchedules(airingAt_greater: $todaySeconds, sort: TIME) {
			id
			media {
				...media
			}
		}
		pageInfo {
			currentPage
			hasNextPage
		}
	}
}
